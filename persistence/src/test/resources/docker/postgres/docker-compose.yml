version: '3.9'

services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    volumes:
      - ~/Work/data/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=test1234
      - POSTGRES_USER=demo
      - POSTGRES_DB=demo_db